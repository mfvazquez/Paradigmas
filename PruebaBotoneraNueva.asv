clc
clear all
WaitSecs(1);
display 'inicio'
esperar = true;
while esperar
    [keyIsDown, secs, keyCode, deltaSecs] = KbCheck;
    find(keyCode)
    if keyIsDown
        esperar = false;
    end
%     WaitSecs(0.1);
end

% 
% [~, keyCode, ~] = KbPressWait;
% find(keyCode)

% PsychHID('KbCheck')